udi_interface >= 3.3.14
markdown2
# aioesphomeapi 45+ needs zeroconf>=0.150 (FreeBSD py311-zeroconf 0.132 is too old).
zeroconf >= 0.150.0, < 2.0
# Home Assistant's ESPHome native API client (TCP :6053). Prefer pure-Python
# build on FreeBSD (see install.sh SKIP_CYTHON=1).
aioesphomeapi >= 45.0.0
